# Flow Cards Expansion Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a condition card, fade duration, relative dim, toggle, snapshot/restore cards, and French translations to the RoomLights Homey app, then release as 1.1.0.

**Architecture:** Everything follows the existing single-file pattern: pure logic as methods on the `RoomLights` class in `app.js`, one JSON file per Flow card in `.homeycompose/flow/`, registration in `onInit()`, and `node --test` tests with a stubbed `homey` runtime in `test/app.test.js`. No drivers, no new dependencies.

**Tech Stack:** Homey SDK 3, homey-api (Web API client), Node.js built-in `node:test`.

## Global Constraints

- Homey compatibility: `>=12.9.0`, platform `local`, SDK `3` (do not change).
- **Edit `.homeycompose/`, never `app.json`** — `app.json` is generated by `homey app build`.
- All copy lives inline in the `.homeycompose` JSON files as `{ "en": ..., "fr": ... }` objects; `locales/en.json` stays empty.
- Code style: `"use strict"`, double quotes, 2-space indent, comments explain *why* not *what* (match `app.js`).
- Card ids are lowercase with no separators, matching existing ids (`setroomlightsrole`).
- Tests run with `npm test` (`node --test`); they stub `homey` and `homey-api` — never require a real Homey.
- Every card with a `room` argument is wrapped in `registerRoomAutocomplete(...)`.
- Dropdown args arrive as an id string or a `{ id }` object — always unwrap with `this.argId(...)`.
- `roomLights(room, role, state)` already excludes `excluded` lights and re-reads live device state only when `state === "on"` — reuse it, never re-implement filtering.
- Commit after every task with a conventional-commit message.

**Verified facts (from Homey docs, 2026-07-29):**
- Action cards get native duration support with `"duration": true` in the card JSON; the run listener receives `args.duration` in **milliseconds** (or `undefined` when the user didn't set one).
- homey-api's `Device.setCapabilityValue` has an object form `setCapabilityValue({ capabilityId, value, duration })` that carries the duration; the positional form `setCapabilityValue(capabilityId, value)` (used everywhere in `app.js` today) cannot.
- Condition cards are registered with `this.homey.flow.getConditionCard(id)` and their run listener returns a boolean; the `!{{on|off}}` token in `titleFormatted` gives the user the inverted variant for free.

---

### Task 1: Condition card — "Any light of the room is on"

**Files:**
- Create: `.homeycompose/flow/conditions/anyroomlightson.json`
- Modify: `app.js` (new method `anyLightsOn`, registration in `onInit`)
- Test: `test/app.test.js`

**Interfaces:**
- Consumes: `roomLights(room, role, state)`, `argId(value)`, `registerRoomAutocomplete(card)` — all existing.
- Produces: `async anyLightsOn(room, role)` → `boolean`. Task 4 (toggle) calls this exact method.

- [ ] **Step 1: Write the failing tests**

Append to `test/app.test.js` (after the `a stale device id in lightRoles is ignored` test, which also uses `roleApp`):

```js
// anyLightsOn backs the condition card and the toggle. It must respect the
// role filter and the excluded role, and read live state, not the snapshot.
test("anyLightsOn is true only when a light of that role is on", async () => {
  const app = await roleApp();
  // Live state: main spot (1) off, ambient strip (2) on, excluded (3) on.
  app.homeyApi.devices.getDevices = async () => ({
    1: { id: 1, zone: "a", class: "light", capabilities: ["onoff"], capabilitiesObj: { onoff: { value: false } } },
    2: { id: 2, zone: "a", class: "light", capabilities: ["onoff"], capabilitiesObj: { onoff: { value: true } } },
    3: { id: 3, zone: "a", class: "light", capabilities: ["onoff"], capabilitiesObj: { onoff: { value: true } } },
  });
  assert.strictEqual(await app.anyLightsOn({ id: "a" }, "ambient"), true);
  assert.strictEqual(await app.anyLightsOn({ id: "a" }, "main"), false);
  assert.strictEqual(await app.anyLightsOn({ id: "a" }, "all"), true);
});

test("anyLightsOn ignores excluded lights even when they are on", async () => {
  const app = await roleApp();
  app.homeyApi.devices.getDevices = async () => ({
    1: { id: 1, zone: "a", class: "light", capabilities: ["onoff"], capabilitiesObj: { onoff: { value: false } } },
    2: { id: 2, zone: "a", class: "light", capabilities: ["onoff"], capabilitiesObj: { onoff: { value: false } } },
    3: { id: 3, zone: "a", class: "light", capabilities: ["onoff"], capabilitiesObj: { onoff: { value: true } } },
  });
  assert.strictEqual(await app.anyLightsOn({ id: "a" }, "all"), false);
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npm test`
Expected: 2 failures with `TypeError: app.anyLightsOn is not a function`.

- [ ] **Step 3: Implement `anyLightsOn` and register the condition card**

In `app.js`, add the method right after `roomLights(...)`:

```js
  // Backs the condition card and the toggle. Note the isOn() convention leaks
  // through here: a light whose onoff state is unknown counts as on.
  async anyLightsOn(room, role) {
    return (await this.roomLights(room, role, "on")).length > 0;
  }
```

In `onInit()`, after the `turnoffroomlights` registration and before the `watchForChanges()` call, add:

```js
    this.registerRoomAutocomplete(
      this.homey.flow.getConditionCard("anyroomlightson").registerRunListener(async (args) => {
        return this.anyLightsOn(args.room, this.argId(args.role));
      })
    );
```

Create `.homeycompose/flow/conditions/anyroomlightson.json` (tab-indented like the other card files):

```json
{
	"title": {
		"en": "Any light is on"
	},
	"hint": {
		"en": "Checks whether any light of the room, filtered by role, is currently on. Excluded lights never count."
	},
	"titleFormatted": {
		"en": "Any [[role]] light of [[room]] is !{{on|off}}"
	},
	"args": [
		{
			"type": "autocomplete",
			"name": "room",
			"title": {
				"en": "Room"
			},
			"placeholder": {
				"en": "Bedroom"
			}
		},
		{
			"type": "dropdown",
			"name": "role",
			"title": {
				"en": "Lights"
			},
			"values": [
				{
					"id": "all",
					"title": {
						"en": "All"
					}
				},
				{
					"id": "main",
					"title": {
						"en": "Main only"
					}
				},
				{
					"id": "ambient",
					"title": {
						"en": "Ambient only"
					}
				}
			]
		}
	]
}
```

- [ ] **Step 4: Update the registration test**

The `a failed event subscription still leaves the Flow cards registered` test stubs `app.homey.flow` with only `getActionCard`. Replace its `flow` stub and expected list:

```js
  app.homey = {
    flow: {
      getActionCard: (id) => {
        registered.push(id);
        return card;
      },
      getConditionCard: (id) => {
        registered.push("condition:" + id);
        return card;
      },
    },
    setTimeout: () => {},
    clearTimeout: () => {},
  };

  await app.onInit();
  assert.deepStrictEqual(registered, [
    "setroomlights",
    "setroomlightscolors",
    "setroomlightsrole",
    "setroomlightscolorsrole",
    "turnoffroomlights",
    "condition:anyroomlightson",
  ]);
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npm test`
Expected: all pass, `# fail 0`.

- [ ] **Step 6: Commit**

```bash
git add app.js test/app.test.js .homeycompose/flow/conditions/anyroomlightson.json
git commit -m "feat: add condition card asking whether any light of a role is on"
```

---

### Task 2: Fade duration on the set-cards

**Files:**
- Modify: `.homeycompose/flow/actions/setroomlightsrole.json` (add `"duration": true`)
- Modify: `.homeycompose/flow/actions/setroomlightscolorsrole.json` (add `"duration": true`)
- Modify: `app.js` (`setLightsBrightness`, `setLightsColors`, both `...role` run listeners, new helper `write`)
- Test: `test/app.test.js`

**Interfaces:**
- Consumes: existing `setLightsBrightness(room, brightness, temperature, options)` and `setLightsColors(room, brightness, color, saturation, options)`.
- Produces: both methods now accept `options.duration` (milliseconds or `undefined`); a new helper `async write(device, capabilityId, value, duration)`. Also: `setLightsBrightness` now skips the `light_temperature` write when `temperature == null` — Task 4 relies on that.

- [ ] **Step 1: Write the failing tests**

Append to `test/app.test.js` (after the `turnOffRoomLights leaves excluded lights alone` test — these reuse `recordingApp`):

```js
test("a duration turns the dim write into a fade", async () => {
  const { app, calls } = recordingApp(["onoff", "dim"]);
  await app.buildRoomLightsZones();
  await app.setLightsBrightness({ id: "a" }, 0.4, 0.5, { duration: 3000 });
  // onoff stays instant — fading onoff is device-dependent; the dim carries
  // the duration through the object form of setCapabilityValue.
  assert.deepStrictEqual(calls, [
    ["onoff", true],
    [{ capabilityId: "dim", value: 0.4, duration: 3000 }, undefined],
  ]);
});

test("brightness 0 with a duration fades to off", async () => {
  const { app, calls } = recordingApp(["onoff", "dim"]);
  await app.buildRoomLightsZones();
  await app.setLightsBrightness({ id: "a" }, 0, 0.5, { duration: 3000 });
  assert.deepStrictEqual(calls, [
    [{ capabilityId: "onoff", value: false, duration: 3000 }, undefined],
  ]);
});

test("no duration keeps the plain positional writes", async () => {
  const { app, calls } = recordingApp(["onoff", "dim"]);
  await app.buildRoomLightsZones();
  await app.setLightsBrightness({ id: "a" }, 0.4, 0.5, {});
  assert.deepStrictEqual(calls, [["onoff", true], ["dim", 0.4]]);
});

test("a null temperature is simply not written", async () => {
  const { app, calls } = recordingApp(["onoff", "dim", "light_temperature"]);
  await app.buildRoomLightsZones();
  await app.setLightsBrightness({ id: "a" }, 0.4, null, {});
  assert.deepStrictEqual(calls, [["onoff", true], ["dim", 0.4]]);
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npm test`
Expected: the duration tests fail (positional `["dim", 0.4]` recorded instead of the object form); the null-temperature test fails with `["light_temperature", null]` recorded.

- [ ] **Step 3: Implement**

In `app.js`, add the helper after `isOn(device)`:

```js
  // Homey fades a capability write when a duration (ms) is given; only the
  // object form of setCapabilityValue carries it. Without a duration, keep the
  // positional form the rest of the app uses.
  async write(device, capabilityId, value, duration) {
    if (duration == null) {
      await device.setCapabilityValue(capabilityId, value);
      return;
    }
    await device.setCapabilityValue({ capabilityId, value, duration });
  }
```

Replace `setLightsBrightness` and `setLightsColors`:

```js
  async setLightsBrightness(room, brightness, temperature, options) {
    const opts = options || {};
    await Promise.all(
      (await this.roomLights(room, opts.role, opts.state)).map(async (device) => {
        if (brightness === 0) {
          await this.write(device, "onoff", false, opts.duration);
          return;
        }
        await device.setCapabilityValue("onoff", true);
        await this.write(device, "dim", brightness, opts.duration);
        if (temperature != null && device.capabilities.includes("light_temperature")) {
          await device.setCapabilityValue("light_temperature", temperature);
        }
      })
    );
  }

  async setLightsColors(room, brightness, color, saturation, options) {
    const opts = options || {};
    await Promise.all(
      (await this.roomLights(room, opts.role, opts.state)).map(async (device) => {
        if (brightness === 0) {
          await this.write(device, "onoff", false, opts.duration);
          return;
        }
        await device.setCapabilityValue("onoff", true);
        await this.write(device, "dim", brightness, opts.duration);
        // Lights without a hue capability just take the brightness.
        if (device.capabilities.includes("light_hue")) {
          await device.setCapabilityValue("light_hue", color);
          await device.setCapabilityValue("light_saturation", saturation);
        }
      })
    );
  }
```

In `onInit()`, thread the duration through the two `...role` run listeners (the deprecated cards keep their exact behaviour — do **not** touch them):

```js
    this.registerRoomAutocomplete(
      this.homey.flow.getActionCard("setroomlightsrole").registerRunListener(async (args) => {
        await this.setLightsBrightness(args.room, args.brightness, args.temperature, {
          role: this.argId(args.role),
          state: this.argId(args.state),
          duration: args.duration,
        });
      })
    );

    this.registerRoomAutocomplete(
      this.homey.flow.getActionCard("setroomlightscolorsrole").registerRunListener(async (args) => {
        await this.setRoomLightsColors(args.room, args.brightness, args.color, {
          role: this.argId(args.role),
          state: this.argId(args.state),
          duration: args.duration,
        });
      })
    );
```

In `.homeycompose/flow/actions/setroomlightsrole.json` and `.homeycompose/flow/actions/setroomlightscolorsrole.json`, add one line after `"titleFormatted"`:

```json
	"duration": true,
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm test`
Expected: all pass, `# fail 0`. The pre-existing test `brightness 0 turns lights off, otherwise dim and temperature are applied` must still pass — it calls with no options, so nothing changes for it.

- [ ] **Step 5: Commit**

```bash
git add app.js test/app.test.js .homeycompose/flow/actions/setroomlightsrole.json .homeycompose/flow/actions/setroomlightscolorsrole.json
git commit -m "feat: native fade duration on the role set-cards"
```

---

### Task 3: Relative dim card

**Files:**
- Create: `.homeycompose/flow/actions/dimroomlights.json`
- Modify: `app.js` (new method `dimRoomLights`, registration in `onInit`)
- Test: `test/app.test.js`

**Interfaces:**
- Consumes: `roomLights(room, role)`, `argId`, `registerRoomAutocomplete`.
- Produces: `async dimRoomLights(room, role, direction, step)` — `direction` is `"up"` or `"down"`, `step` is 0–1.

- [ ] **Step 1: Write the failing tests**

Append to `test/app.test.js`:

```js
// One fresh read backs the whole relative-dim run: current onoff and dim come
// from the same getDevices() call, never from the myHome snapshot.
function dimApp(capabilitiesObj) {
  const calls = [];
  const bulb = {
    id: 1,
    zone: "a",
    class: "light",
    name: "spot",
    capabilities: ["onoff", "dim"],
    setCapabilityValue: async (cap, value) => calls.push([cap, value]),
  };
  const app = fakeApp({
    zones: { a: { id: "a", name: "Salon", parent: null } },
    devices: { 1: bulb },
  });
  app.homeyApi.devices.getDevices = async () => ({
    1: { id: 1, zone: "a", class: "light", capabilities: ["onoff", "dim"], capabilitiesObj },
  });
  return { app, calls };
}

test("dim up raises the brightness and clamps at 1", async () => {
  const { app, calls } = dimApp({ onoff: { value: true }, dim: { value: 0.9 } });
  await app.buildRoomLightsZones();
  await app.dimRoomLights({ id: "a" }, "all", "up", 0.2);
  assert.deepStrictEqual(calls, [["dim", 1]]);
});

test("dim down to zero turns the light off instead of dim 0", async () => {
  const { app, calls } = dimApp({ onoff: { value: true }, dim: { value: 0.1 } });
  await app.buildRoomLightsZones();
  await app.dimRoomLights({ id: "a" }, "all", "down", 0.2);
  assert.deepStrictEqual(calls, [["onoff", false]]);
});

test("relative dim leaves lights that are off untouched", async () => {
  const { app, calls } = dimApp({ onoff: { value: false }, dim: { value: 0.5 } });
  await app.buildRoomLightsZones();
  await app.dimRoomLights({ id: "a" }, "all", "up", 0.2);
  assert.deepStrictEqual(calls, []);
});

test("relative dim skips a light whose dim value is unreadable", async () => {
  const { app, calls } = dimApp({ onoff: { value: true } });
  await app.buildRoomLightsZones();
  await app.dimRoomLights({ id: "a" }, "all", "up", 0.2);
  assert.deepStrictEqual(calls, []);
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npm test`
Expected: 4 failures with `TypeError: app.dimRoomLights is not a function`.

- [ ] **Step 3: Implement `dimRoomLights` and register the card**

In `app.js`, add after `anyLightsOn`:

```js
  // Relative dim only touches lights that are already on: dimming "up" must
  // not wake a lamp nobody switched on. Unlike isOn(), an unreadable state
  // means skip — a relative change needs a base value to be meaningful.
  async dimRoomLights(room, role, direction, step) {
    const fresh = await this.homeyApi.devices.getDevices();
    const delta = direction === "down" ? -step : step;
    await Promise.all(
      (await this.roomLights(room, role)).map(async (device) => {
        const caps = fresh[device.id] && fresh[device.id].capabilitiesObj;
        if (caps == null || caps.onoff == null || caps.onoff.value !== true) {
          return;
        }
        if (caps.dim == null || typeof caps.dim.value !== "number") {
          return;
        }
        const dim = Math.min(1, Math.max(0, caps.dim.value + delta));
        if (dim === 0) {
          await device.setCapabilityValue("onoff", false);
          return;
        }
        await device.setCapabilityValue("dim", dim);
      })
    );
  }
```

In `onInit()`, after the `anyroomlightson` registration:

```js
    this.registerRoomAutocomplete(
      this.homey.flow.getActionCard("dimroomlights").registerRunListener(async (args) => {
        await this.dimRoomLights(args.room, this.argId(args.role), this.argId(args.direction), args.step);
      })
    );
```

Create `.homeycompose/flow/actions/dimroomlights.json`:

```json
{
	"title": {
		"en": "Dim room lights up or down"
	},
	"hint": {
		"en": "Raises or lowers the brightness of the room's lights that are currently on. Reaching zero turns a light off; lights that are off stay off."
	},
	"titleFormatted": {
		"en": "Dim [[role]] lights of [[room]] [[direction]] by [[step]]"
	},
	"args": [
		{
			"type": "autocomplete",
			"name": "room",
			"title": {
				"en": "Room"
			},
			"placeholder": {
				"en": "Bedroom"
			}
		},
		{
			"type": "dropdown",
			"name": "role",
			"title": {
				"en": "Lights"
			},
			"values": [
				{
					"id": "all",
					"title": {
						"en": "All"
					}
				},
				{
					"id": "main",
					"title": {
						"en": "Main only"
					}
				},
				{
					"id": "ambient",
					"title": {
						"en": "Ambient only"
					}
				}
			]
		},
		{
			"type": "dropdown",
			"name": "direction",
			"title": {
				"en": "Direction"
			},
			"values": [
				{
					"id": "up",
					"title": {
						"en": "up"
					}
				},
				{
					"id": "down",
					"title": {
						"en": "down"
					}
				}
			]
		},
		{
			"type": "range",
			"name": "step",
			"title": "step",
			"min": 0,
			"max": 1,
			"step": 0.01,
			"label": "%",
			"labelDecimals": 0,
			"labelMultiplier": 100
		}
	]
}
```

- [ ] **Step 4: Update the registration test**

In the registration test, the expected list becomes:

```js
  assert.deepStrictEqual(registered, [
    "setroomlights",
    "setroomlightscolors",
    "setroomlightsrole",
    "setroomlightscolorsrole",
    "turnoffroomlights",
    "condition:anyroomlightson",
    "dimroomlights",
  ]);
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npm test`
Expected: all pass, `# fail 0`.

- [ ] **Step 6: Commit**

```bash
git add app.js test/app.test.js .homeycompose/flow/actions/dimroomlights.json
git commit -m "feat: add relative dim card for wall buttons and remotes"
```

---

### Task 4: Toggle card

**Files:**
- Create: `.homeycompose/flow/actions/toggleroomlights.json`
- Modify: `app.js` (new method `toggleRoomLights`, registration in `onInit`)
- Test: `test/app.test.js`

**Interfaces:**
- Consumes: `anyLightsOn(room, role)` (Task 1), `turnOffRoomLights(room, role)` (existing), `setLightsBrightness(room, brightness, null, { role })` with the null-temperature guard (Task 2).
- Produces: `async toggleRoomLights(room, role, brightness)`.

- [ ] **Step 1: Write the failing tests**

Append to `test/app.test.js`:

```js
function toggleApp(onoffValue) {
  const calls = [];
  const bulb = {
    id: 1,
    zone: "a",
    class: "light",
    name: "spot",
    capabilities: ["onoff", "dim", "light_temperature"],
    setCapabilityValue: async (cap, value) => calls.push([cap, value]),
  };
  const app = fakeApp({
    zones: { a: { id: "a", name: "Salon", parent: null } },
    devices: { 1: bulb },
  });
  app.homeyApi.devices.getDevices = async () => ({
    1: { ...bulb, capabilitiesObj: { onoff: { value: onoffValue } } },
  });
  return { app, calls };
}

test("toggle turns everything off when any light is on", async () => {
  const { app, calls } = toggleApp(true);
  await app.buildRoomLightsZones();
  await app.toggleRoomLights({ id: "a" }, "all", 0.6);
  assert.deepStrictEqual(calls, [["onoff", false]]);
});

test("toggle turns the lights on at the given brightness, without touching temperature", async () => {
  const { app, calls } = toggleApp(false);
  await app.buildRoomLightsZones();
  await app.toggleRoomLights({ id: "a" }, "all", 0.6);
  assert.deepStrictEqual(calls, [["onoff", true], ["dim", 0.6]]);
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npm test`
Expected: 2 failures with `TypeError: app.toggleRoomLights is not a function`.

- [ ] **Step 3: Implement `toggleRoomLights` and register the card**

In `app.js`, add after `dimRoomLights`:

```js
  async toggleRoomLights(room, role, brightness) {
    if (await this.anyLightsOn(room, role)) {
      await this.turnOffRoomLights(room, role);
      return;
    }
    // No temperature: the lights come on at the given brightness and keep
    // whatever colour or temperature they last had.
    await this.setLightsBrightness(room, brightness, null, { role });
  }
```

In `onInit()`, after the `dimroomlights` registration:

```js
    this.registerRoomAutocomplete(
      this.homey.flow.getActionCard("toggleroomlights").registerRunListener(async (args) => {
        await this.toggleRoomLights(args.room, this.argId(args.role), args.brightness);
      })
    );
```

Create `.homeycompose/flow/actions/toggleroomlights.json`:

```json
{
	"title": {
		"en": "Toggle room lights"
	},
	"hint": {
		"en": "If any light of the role is on, turns them all off. Otherwise turns them on at the given brightness."
	},
	"titleFormatted": {
		"en": "Toggle [[role]] lights of [[room]] (on at brightness [[brightness]])"
	},
	"args": [
		{
			"type": "autocomplete",
			"name": "room",
			"title": {
				"en": "Room"
			},
			"placeholder": {
				"en": "Bedroom"
			}
		},
		{
			"type": "dropdown",
			"name": "role",
			"title": {
				"en": "Lights"
			},
			"values": [
				{
					"id": "all",
					"title": {
						"en": "All"
					}
				},
				{
					"id": "main",
					"title": {
						"en": "Main only"
					}
				},
				{
					"id": "ambient",
					"title": {
						"en": "Ambient only"
					}
				}
			]
		},
		{
			"type": "range",
			"name": "brightness",
			"title": "brightness",
			"min": 0,
			"max": 1,
			"step": 0.01,
			"label": "%",
			"labelDecimals": 0,
			"labelMultiplier": 100
		}
	]
}
```

- [ ] **Step 4: Update the registration test**

Expected list becomes:

```js
  assert.deepStrictEqual(registered, [
    "setroomlights",
    "setroomlightscolors",
    "setroomlightsrole",
    "setroomlightscolorsrole",
    "turnoffroomlights",
    "condition:anyroomlightson",
    "dimroomlights",
    "toggleroomlights",
  ]);
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npm test`
Expected: all pass, `# fail 0`.

- [ ] **Step 6: Commit**

```bash
git add app.js test/app.test.js .homeycompose/flow/actions/toggleroomlights.json
git commit -m "feat: add toggle card driven by live room state"
```

---

### Task 5: Snapshot and restore cards

**Files:**
- Create: `.homeycompose/flow/actions/saveroomlights.json`
- Create: `.homeycompose/flow/actions/restoreroomlights.json`
- Modify: `app.js` (methods `saveRoomLights`, `restoreRoomLights`, registration in `onInit`)
- Test: `test/app.test.js`

**Interfaces:**
- Consumes: `roomLights(room)` (no role/state — a snapshot covers the whole room minus excluded), `this.homey.settings` under a new key `"lightSnapshots"`.
- Produces: `async saveRoomLights(room)` and `async restoreRoomLights(room)`. Settings shape: `{ [zoneId]: { [deviceId]: { onoff: boolean, dim?: number, light_temperature?: number, light_hue?: number, light_saturation?: number } } }`.

- [ ] **Step 1: Write the failing tests**

Append to `test/app.test.js`:

```js
function snapshotApp() {
  const calls = [];
  const caps = ["onoff", "dim", "light_hue", "light_saturation"];
  const record = (id) => async (cap, value) => calls.push([id, cap, value]);
  const app = fakeApp({
    zones: { a: { id: "a", name: "Salon", parent: null } },
    devices: {
      1: { id: 1, zone: "a", class: "light", name: "spot", capabilities: caps, setCapabilityValue: record(1) },
      2: { id: 2, zone: "a", class: "light", name: "strip", capabilities: caps, setCapabilityValue: record(2) },
    },
  });
  // Live state at save time: spot on and warm, strip off.
  app.homeyApi.devices.getDevices = async () => ({
    1: {
      id: 1, zone: "a", class: "light", capabilities: caps,
      capabilitiesObj: { onoff: { value: true }, dim: { value: 0.4 }, light_hue: { value: 0.1 }, light_saturation: { value: 1 } },
    },
    2: {
      id: 2, zone: "a", class: "light", capabilities: caps,
      capabilitiesObj: { onoff: { value: false }, dim: { value: 0.8 } },
    },
  });
  return { app, calls };
}

test("restore replays the saved state of every light", async () => {
  const { app, calls } = snapshotApp();
  await app.buildRoomLightsZones();
  await app.saveRoomLights({ id: "a" });
  await app.restoreRoomLights({ id: "a" });
  assert.deepStrictEqual(calls.sort(), [
    [1, "dim", 0.4],
    [1, "light_hue", 0.1],
    [1, "light_saturation", 1],
    [1, "onoff", true],
    [2, "onoff", false],
  ].sort());
});

test("restore without a snapshot is a no-op", async () => {
  const { app, calls } = snapshotApp();
  await app.buildRoomLightsZones();
  await app.restoreRoomLights({ id: "a" });
  assert.deepStrictEqual(calls, []);
});

test("a light removed after the save is skipped on restore", async () => {
  const { app, calls } = snapshotApp();
  await app.buildRoomLightsZones();
  await app.saveRoomLights({ id: "a" });
  // The strip disappears from the zone before the restore.
  app.myHome["a"].devices["light"] = app.myHome["a"].devices["light"].filter((d) => d.id !== 2);
  await app.restoreRoomLights({ id: "a" });
  assert.ok(calls.every((c) => c[0] === 1), "only the surviving light may be written to");
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npm test`
Expected: 3 failures with `TypeError: app.saveRoomLights is not a function` (or `restoreRoomLights` for the second test).

- [ ] **Step 3: Implement and register**

In `app.js`, add after `toggleRoomLights`:

```js
  // A snapshot covers the whole room minus excluded lights — save/restore is
  // "movie mode, then back to what it was", not a role tool. Snapshots live in
  // app settings so they survive an app restart.
  async saveRoomLights(room) {
    const fresh = await this.homeyApi.devices.getDevices();
    const snapshot = {};
    for (const device of await this.roomLights(room)) {
      const caps = fresh[device.id] && fresh[device.id].capabilitiesObj;
      if (caps == null) {
        continue;
      }
      const entry = { onoff: caps.onoff == null ? true : caps.onoff.value === true };
      for (const cap of ["dim", "light_temperature", "light_hue", "light_saturation"]) {
        if (caps[cap] != null && typeof caps[cap].value === "number") {
          entry[cap] = caps[cap].value;
        }
      }
      snapshot[device.id] = entry;
    }
    const all = this.homey.settings.get("lightSnapshots") || {};
    all[room.id] = snapshot;
    this.homey.settings.set("lightSnapshots", all);
  }

  async restoreRoomLights(room) {
    const all = this.homey.settings.get("lightSnapshots") || {};
    const snapshot = all[room.id];
    if (snapshot == null) {
      return;
    }
    await Promise.all(
      (await this.roomLights(room)).map(async (device) => {
        const saved = snapshot[device.id];
        if (saved == null) {
          return;
        }
        if (saved.onoff !== true) {
          await device.setCapabilityValue("onoff", false);
          return;
        }
        await device.setCapabilityValue("onoff", true);
        for (const cap of ["dim", "light_temperature", "light_hue", "light_saturation"]) {
          if (saved[cap] != null && device.capabilities.includes(cap)) {
            await device.setCapabilityValue(cap, saved[cap]);
          }
        }
      })
    );
  }
```

In `onInit()`, after the `toggleroomlights` registration:

```js
    this.registerRoomAutocomplete(
      this.homey.flow.getActionCard("saveroomlights").registerRunListener(async (args) => {
        await this.saveRoomLights(args.room);
      })
    );

    this.registerRoomAutocomplete(
      this.homey.flow.getActionCard("restoreroomlights").registerRunListener(async (args) => {
        await this.restoreRoomLights(args.room);
      })
    );
```

Create `.homeycompose/flow/actions/saveroomlights.json`:

```json
{
	"title": {
		"en": "Save the room lights"
	},
	"hint": {
		"en": "Remembers the current on/off, brightness and colour of every light in the room, replacing the previous snapshot. Restore it later with the restore card."
	},
	"titleFormatted": {
		"en": "Save the lights of [[room]]"
	},
	"args": [
		{
			"type": "autocomplete",
			"name": "room",
			"title": {
				"en": "Room"
			},
			"placeholder": {
				"en": "Bedroom"
			}
		}
	]
}
```

Create `.homeycompose/flow/actions/restoreroomlights.json`:

```json
{
	"title": {
		"en": "Restore the room lights"
	},
	"hint": {
		"en": "Puts every light of the room back to the state remembered by the save card. Does nothing when no snapshot exists."
	},
	"titleFormatted": {
		"en": "Restore the lights of [[room]]"
	},
	"args": [
		{
			"type": "autocomplete",
			"name": "room",
			"title": {
				"en": "Room"
			},
			"placeholder": {
				"en": "Bedroom"
			}
		}
	]
}
```

- [ ] **Step 4: Update the registration test**

Final expected list:

```js
  assert.deepStrictEqual(registered, [
    "setroomlights",
    "setroomlightscolors",
    "setroomlightsrole",
    "setroomlightscolorsrole",
    "turnoffroomlights",
    "condition:anyroomlightson",
    "dimroomlights",
    "toggleroomlights",
    "saveroomlights",
    "restoreroomlights",
  ]);
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npm test`
Expected: all pass, `# fail 0`.

- [ ] **Step 6: Commit**

```bash
git add app.js test/app.test.js .homeycompose/flow/actions/saveroomlights.json .homeycompose/flow/actions/restoreroomlights.json
git commit -m "feat: add save and restore cards for room light snapshots"
```

---

### Task 6: French translations

**Files:**
- Modify: `.homeycompose/app.json` (`description` gets `fr`)
- Modify: every file in `.homeycompose/flow/actions/` and `.homeycompose/flow/conditions/` (add `fr` beside every `en`)

**Interfaces:** none — pure copy. The settings page (`settings/index.html`) has no i18n mechanism and stays English; do not touch it.

- [ ] **Step 1: Add `fr` beside every `en` string**

Every `{ "en": "..." }` object in the files above becomes `{ "en": "...", "fr": "..." }`. Card names (`titleFormatted` argument tokens like `[[room]]`) stay untouched inside the strings. Use exactly these translations:

**Manifest (`.homeycompose/app.json`):**

| Field | en | fr |
|---|---|---|
| description | Let you control all the lights in a defined Homey zone at once | Contrôlez d'un coup toutes les lumières d'une zone Homey |

**Shared argument strings (every card that has them):**

| en | fr |
|---|---|
| Room | Pièce |
| Bedroom *(placeholder)* | Chambre |
| Lights | Lumières |
| All | Toutes |
| Main only | Principales uniquement |
| Ambient only | Ambiance uniquement |
| Apply to | Appliquer à |
| All lights | Toutes les lumières |
| Only lights already on | Seulement les lumières déjà allumées |

**`setroomlights.json` (deprecated card — translate anyway, existing Flows show it):**

| Field | fr |
|---|---|
| title "Set room lights" | Régler les lumières de la pièce |
| titleFormatted | Régler les lumières de [[room]] sur luminosité [[brightness]] et température [[temperature]] |

**`setroomlightscolors.json` (deprecated):**

| Field | fr |
|---|---|
| title "Set room lights color" | Régler la couleur des lumières de la pièce |
| titleFormatted | Régler les lumières de [[room]] sur luminosité [[brightness]] et couleur [[color]] |

*(Check the actual `en` strings in these two files and mirror their structure — the `fr` lines above cover the intent; adjust wording only if an `en` string differs from what is listed.)*

**`setroomlightsrole.json`:**

| Field | fr |
|---|---|
| title "Set room lights" | Régler les lumières de la pièce |
| hint | Règle la luminosité et la température des lumières d'une pièce, filtrées par rôle et selon qu'elles sont déjà allumées. |
| titleFormatted | Régler les lumières [[role]] de [[room]] ([[state]]) sur luminosité [[brightness]] et température [[temperature]] |

**`setroomlightscolorsrole.json`:**

| Field | fr |
|---|---|
| title | Régler la couleur des lumières de la pièce |
| hint | Règle la luminosité et la couleur des lumières d'une pièce, filtrées par rôle et selon qu'elles sont déjà allumées. |
| titleFormatted | Régler les lumières [[role]] de [[room]] ([[state]]) sur luminosité [[brightness]] et couleur [[color]] |

**`turnoffroomlights.json`:**

| Field | fr |
|---|---|
| title "Turn off room lights" | Éteindre les lumières de la pièce |
| hint | Éteint les lumières de la pièce, filtrées par rôle. |
| titleFormatted | Éteindre les lumières [[role]] de [[room]] |

**`anyroomlightson.json`:**

| Field | fr |
|---|---|
| title | Une lumière est allumée |
| hint | Vérifie si au moins une lumière de la pièce, filtrée par rôle, est allumée. Les lumières exclues ne comptent jamais. |
| titleFormatted | Une lumière [[role]] de [[room]] est !{{allumée\|éteinte}} |

**`dimroomlights.json`:**

| Field | fr |
|---|---|
| title | Varier les lumières de la pièce |
| hint | Augmente ou diminue la luminosité des lumières déjà allumées. Atteindre zéro éteint la lumière ; les lumières éteintes le restent. |
| titleFormatted | Varier les lumières [[role]] de [[room]] vers [[direction]] de [[step]] |
| direction "up" | le haut |
| direction "down" | le bas |
| direction title "Direction" | Direction |

**`toggleroomlights.json`:**

| Field | fr |
|---|---|
| title | Basculer les lumières de la pièce |
| hint | Si une lumière du rôle est allumée, les éteint toutes. Sinon les allume à la luminosité donnée. |
| titleFormatted | Basculer les lumières [[role]] de [[room]] (allumage à [[brightness]]) |

**`saveroomlights.json`:**

| Field | fr |
|---|---|
| title | Mémoriser les lumières de la pièce |
| hint | Mémorise l'état actuel (allumé, luminosité, couleur) de chaque lumière de la pièce, en remplaçant la mémorisation précédente. |
| titleFormatted | Mémoriser les lumières de [[room]] |

**`restoreroomlights.json`:**

| Field | fr |
|---|---|
| title | Rétablir les lumières de la pièce |
| hint | Remet chaque lumière de la pièce dans l'état mémorisé par la carte de mémorisation. Ne fait rien sans mémorisation. |
| titleFormatted | Rétablir les lumières de [[room]] |

Range args like `brightness` / `temperature` / `step` have a plain-string `"title"` (not an `{ en }` object) in the existing files — leave those as they are.

Example of the finished shape (top of `turnoffroomlights.json`):

```json
{
	"title": {
		"en": "Turn off room lights",
		"fr": "Éteindre les lumières de la pièce"
	},
	"titleFormatted": {
		"en": "Turn off [[role]] lights of [[room]]",
		"fr": "Éteindre les lumières [[role]] de [[room]]"
	},
```

- [ ] **Step 2: Verify the JSON is still valid**

Run: `node -e "for (const f of require('fs').readdirSync('.homeycompose/flow/actions')) JSON.parse(require('fs').readFileSync('.homeycompose/flow/actions/' + f)); JSON.parse(require('fs').readFileSync('.homeycompose/flow/conditions/anyroomlightson.json')); JSON.parse(require('fs').readFileSync('.homeycompose/app.json')); console.log('all valid')"`
Expected: `all valid`

- [ ] **Step 3: Run tests**

Run: `npm test`
Expected: all pass — translations touch no code.

- [ ] **Step 4: Commit**

```bash
git add .homeycompose
git commit -m "feat: French translations for the manifest and every Flow card"
```

---

### Task 7: Docs, changelog and release 1.1.0

**Files:**
- Modify: `README.md` (Flow cards section + key-methods table)
- Modify: `.homeychangelog.json`
- Modify: `.homeycompose/app.json` (`version`)
- Modify: `package.json` (`version`)

**Interfaces:** none.

- [ ] **Step 1: Document the new cards in `README.md`**

In the *Flow cards* section, after the `turnoffroomlights` entry and before the *Deprecated* entry, add:

```markdown
### `anyroomlightson` — condition: "Any \[role] light of \[room] is on"

The app's only **condition** card (the *…and* column). True when at least one light of the role is currently on — live state, not a cached snapshot. Excluded lights never count. Homey offers the inverted "…is off" variant automatically. A light whose on/off state cannot be read counts as on, consistent with the state filter on the set-cards.

### `dimroomlights` — "Dim \[role] lights of \[room] \[direction] by \[step]"

Relative dimming for wall buttons and dial remotes. Only touches lights that are already on: dimming *up* never wakes a lamp nobody switched on. Reaching zero turns the light off; a light whose current brightness cannot be read is skipped.

### `toggleroomlights` — "Toggle \[role] lights of \[room] (on at brightness \[brightness])"

If any light of the role is on, turns them all off; otherwise turns them on at the given brightness, keeping whatever colour or temperature they last had.

### `saveroomlights` / `restoreroomlights` — "Save/Restore the lights of \[room]"

The movie-mode pair. *Save* remembers on/off, brightness and colour of every light in the room (minus excluded) in app settings, replacing the room's previous snapshot; *restore* replays it. A light removed since the save is skipped; restoring a room that was never saved does nothing.

### Fade duration

`setroomlightsrole` and `setroomlightscolorsrole` support Homey's native duration picker. With a duration set, the brightness write becomes a fade; the on/off write stays instant, so a light that was off jumps on and then fades to the target. The deprecated cards do not get durations.
```

In the *Architecture* key-methods table, add rows:

```markdown
| `anyLightsOn(room, role)` | **async.** True when any non-excluded light of the role is on — backs the condition card and the toggle |
| `dimRoomLights(room, role, direction, step)` | Relative dim of the lights currently on, clamped to 0–1; zero turns off |
| `toggleRoomLights(room, role, brightness)` | All off if anything is on, otherwise on at the given brightness |
| `saveRoomLights(room)` / `restoreRoomLights(room)` | Persist and replay a per-room snapshot in the `lightSnapshots` setting |
| `write(device, capabilityId, value, duration)` | One capability write, as a fade when a duration (ms) is given |
```

- [ ] **Step 2: Bump versions and changelog**

In `.homeycompose/app.json` and `package.json`, set `"version": "1.1.0"`.

In `.homeychangelog.json`, add:

```json
	"1.1.0": {
		"en": "New cards: any-light-on condition, relative dim, toggle, save & restore. Fade duration on the set-cards. French translations.",
		"fr": "Nouvelles cartes : condition lumière allumée, variation relative, bascule, mémoriser & rétablir. Durée de fondu sur les cartes de réglage. Traductions françaises."
	}
```

- [ ] **Step 3: Run the full test suite one last time**

Run: `npm test`
Expected: `# fail 0`.

If the Homey CLI is installed, also run: `homey app validate --level publish`
Expected: validation OK. (Skip without failing the task if the CLI is not available — note it in the commit body.)

- [ ] **Step 4: Commit**

```bash
git add README.md .homeychangelog.json .homeycompose/app.json package.json
git commit -m "docs: document the new cards and release 1.1.0"
```

---

## Execution notes

- Tasks 1→5 each ship a working app on their own; 6 and 7 are copy-only. Task 4 depends on Task 1 (`anyLightsOn`) and Task 2 (null-temperature guard); Task 6 depends on all card files existing; otherwise order is free.
- The registration test's expected list changes in Tasks 1, 3, 4 and 5 — each task shows its exact expected list; use the one from the task you are on.
- Do not modify the two deprecated cards' behaviour or arguments in any task.
